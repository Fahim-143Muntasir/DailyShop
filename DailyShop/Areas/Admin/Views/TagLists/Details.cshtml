@model DailyShop.Models.TagLists
@{
    ViewData["Title"] = "Details";
}
<br />

<h2 class="text-success">TagLists Details</h2>
<form asp-action="Details" method="post">
    <div class="p-4 rounded border">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group row">
            <div class="col-2">
                <label asp-for="TagList"></label>
            </div>
            <div class="col-3">
                <input asp-for="TagList" readonly="readonly" class="form-control" />
            </div>
            <span asp-validation-for="TagList" class="text-danger"></span>
        </div>
        <div class="form-group">
            <a asp-action="Edit" asp-route-id="@Model.Id" asp-controller="TagLists" class="btn btn-success">Edit</a>
            @*<a asp-action="Index" class="btn btn-primary">Back To List</a>*@
            <input type="submit" class="btn btn-primary" value="Back To List" />
        </div>
    </div>
</form>
@section Scripts{
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}